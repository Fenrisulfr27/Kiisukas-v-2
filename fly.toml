app = "kiisukas-v-2"
primary_region = "arn"

[build]
  builder = "heroku/builder:22"

[deploy]
  strategy = "immediate"

[mounts]
  source = "bot_data"
  destination = "/data"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1